apiVersion: v1
kind: Service
metadata:
  name: {{ include "libro.name" . }}
  labels:
    {{- include "libro.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "libro.selectorLabels" . | nindent 4 }}
